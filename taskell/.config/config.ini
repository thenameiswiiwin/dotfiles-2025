[general]
filename = taskell.md  # File where tasks are stored

[layout]
padding = 1            # Padding between columns
column_width = 30      # Width of each column
column_padding = 3     # Space between columns
description_indicator = "≡"  # Symbol to indicate a description
statusbar = true       # Enable or disable the status bar

[markdown]
title = "##"           # Markdown syntax for column titles
task = "-"             # Markdown syntax for tasks
summary = "    >"      # Markdown syntax for task summaries
due = "    @"          # Markdown syntax for due dates
subtask = "    *"      # Markdown syntax for subtasks
localTimes = false     # Use local times for due dates
