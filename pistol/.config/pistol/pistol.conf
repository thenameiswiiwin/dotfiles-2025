# Filetype handling configuration

# For Markdown files (.mdx), use bat with specific options
fpath .mdx$ bat --paging=never --color=always --style=plain "%pistol-filename%"

# For plain text files, use bat with default settings
text/plain bat --paging=never --color=always "%pistol-filename%"
